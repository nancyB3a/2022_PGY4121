import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EjemploPGY4121',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
